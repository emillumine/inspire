<% @recommendations.each do |reco| %>
	<section class=reco.type>
		<article>
			<header>
				<%= reco.user.username %>
				<h1><%= reco.title %></h1>
			</header>
			<%= reco.content %>
			<footer>
				<%= reco.published_at %>
			</footer>
		</article>
		<aside>
		</aside>
	</section>
<% end %>